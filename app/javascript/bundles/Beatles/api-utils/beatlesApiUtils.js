// import rp from 'request';

const LYRICS_LIST_URI = `${window.location.protocol}//${window.location.host}/recurrences`

export const requestLyrics = function (data) {
  debugger
  const uri = LYRICS_LIST_URI
  // return rp(uri, data)
}
